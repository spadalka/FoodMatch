import React from "react";
/*import { IonToolbar, IonHeader, IonTitle, IonIcon } from "@ionic/react";
import {closeOutline} from 'ionicons/icons';

const ModalSubscreen: React.FC = () => {
    return(
        <IonHeader>
            <IonToolbar color="danger">
              <IonTitle color="dark" class="ion-padding">
                Recipe
              </IonTitle>
              <IonIcon slot="end" icon={closeOutline} color="dark" size="large" onClick={()=> setShowModal(false)} class="ion-padding"></IonIcon>
            </IonToolbar>
        </IonHeader>
    )
}

export default ModalSubscreen;*/